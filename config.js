// Copy this file to `config.js` and set your production backend URL.
// Example: window.SHOWDO_CONFIG = { serverBase: 'https://show-do-miau.onrender.com' }

window.SHOWDO_CONFIG = window.SHOWDO_CONFIG || {};
window.SHOWDO_CONFIG.serverBase = "https://api-quizia.farigab.com";

// NOTE: This file is safe to commit (it only exposes a public URL). If you
// prefer not to commit a production config, create `config.js` locally or
// generate it during your Pages build step from an environment variable.
